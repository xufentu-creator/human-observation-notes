# v1.2.1 — Final Static Discovery and Sharing Edition

- Generates a complete static article directory from `daily/`.
- Generates one independent HTML page for every observation.
- Generates the complete archive, sitemap, RSS, canonical URLs, and structured article metadata.
- Preserves original Markdown files, dates, paths, and Git history.
- Adds a reading-completion sharing panel with native share, X, LinkedIn, and copy-link actions.
- Keeps source, history, immutable version, SHA-256, citation, and print/PDF access on each article.
